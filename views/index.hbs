<div class="container mt-5 px-4">
  <div class="row d-flex justify-content-center text-center">
    <h1 style="font-weight: bold; font-size: clamp(32px, 8vw, 64px); letter-spacing: 2px;">縮網址神器</h1>
    <p style="font-size: clamp(14px, 5vw, 16px);">這是一個簡單的縮網址服務，可以將長網址轉換為短網址</p>
    <img src="https://assets-lighthouse.alphacamp.co/uploads/image/file/16716/ExportedContentImage_00.png" alt="縮網址神器"
      style="width: clamp(150px, 15vw, 250px); height: auto; margin: 40px 0;">
  </div>
  <div class="row d-flex flex-column align-items-center mb-5">
    <div class="text-center mb-4" style="width: clamp(300px, 50vw, 500px);">
      <input class="form-control" type="text" name="url" placeholder="請輸入網址..." value="{{url}}">
    </div>
    <form action="/urlshortener/newurl" method="GET" class="text-center">
      <button type="submit" class="btn btn-outline-primary mb-3">Shorten!</button>
    </form>
  </div>
</div>